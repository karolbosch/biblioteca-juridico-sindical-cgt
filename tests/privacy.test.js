import test from"node:test";import assert from"node:assert/strict";import{detectSensitiveText,initialPrivacyStatus,mayPublish}from"../worker/src/privacy.js";import{detectFile,safeFilename}from"../worker/src/security.js";
test("detecta identificadores frecuentes",()=>{const f=detectSensitiveText("DNI 12345678Z, correo persona@example.org e IBAN ES9121000418450200051332");assert.deepEqual(new Set(f.map(x=>x.type)),new Set(["DNI","EMAIL","IBAN"]))});
test("una subida nunca nace publicable",()=>{assert.equal(initialPrivacyStatus("pdf"),"ANONIMIZACION_PENDIENTE");assert.equal(mayPublish({privacy_status:"ANONIMIZACION_VERIFICADA",human_verified:0,residual_findings:0}),false);assert.equal(mayPublish({privacy_status:"ANONIMIZACION_VERIFICADA",human_verified:1,residual_findings:0}),true)});
test("valida firma y extensión, y sanea nombres",()=>{assert.equal(detectFile(new TextEncoder().encode("%PDF-1.7"),"application/pdf","sentencia.pdf"),"pdf");assert.equal(detectFile(new TextEncoder().encode("hello"),"application/pdf","sentencia.pdf"),null);assert.equal(safeFilename("../../mi sentencia<script>.pdf"),"_.._mi_sentencia_script_.pdf")});
