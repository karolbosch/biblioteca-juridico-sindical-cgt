import test from"node:test";import assert from"node:assert/strict";import{isAdverse,legalRank,ruleBasedAnswer}from"../worker/src/search.js";
test("una resolución revocada nunca supera al criterio final",()=>{const a={title:"Sentencia favorable sobre X",matter:"X",court_level:"JS",procedural_status:"REVOCADA",year:2025};const b={title:"Resolución posterior desfavorable sobre X",matter:"X",court_level:"TSJ",procedural_status:"CONFIRMADA_FIRME",final_authority:"TSJ",year:2026};assert.ok(legalRank(b,"jurisprudencia favorable sobre X")>legalRank(a,"jurisprudencia favorable sobre X"));assert.equal(isAdverse(a.procedural_status),true)});
test("el fallback responde con criterio actual sin describir el inventario",()=>{const sources=[{title:"B posterior",procedural_status:"CONFIRMADA_FIRME",current_rule_summary:"Criterio actual"},{title:"A inicial",procedural_status:"REVOCADA"}];const answer=ruleBasedAnswer("X",sources);assert.match(answer,/Criterio actual/);assert.match(answer,/revocado/);assert.doesNotMatch(answer,/documentos encontrados|fichas encontradas/i)});
test("sin resultados no inventa jurisprudencia",()=>{assert.match(ruleBasedAnswer("tema",[]),/No he encontrado documentación suficiente/) });
