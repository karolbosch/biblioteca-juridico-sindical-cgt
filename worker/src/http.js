export function cors(env,request){const allowed=env.ALLOWED_ORIGIN||"";const origin=request.headers.get("origin")||"";const ok=allowed.split(",").map(x=>x.trim()).filter(Boolean).includes(origin);return {"access-control-allow-origin":ok?origin:allowed.split(",")[0]||"null","access-control-allow-credentials":"true","access-control-allow-headers":"content-type,x-csrf-token","access-control-allow-methods":"GET,POST,PATCH,OPTIONS","vary":"Origin","x-content-type-options":"nosniff","referrer-policy":"no-referrer","content-security-policy":"default-src 'none'; frame-ancestors 'none'","cache-control":"no-store"}}
export function json(body,status=200,headers={}){return new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8",...headers}})}
export function error(message,status,headers){return json({error:message},status,headers)}
export function assertAllowedOrigin(request,env){const origin=request.headers.get("origin");const allowed=(env.ALLOWED_ORIGIN||"").split(",").map(x=>x.trim()).filter(Boolean);return !origin||allowed.includes(origin)}
export async function readJson(request,max=16384){const length=Number(request.headers.get("content-length")||0);if(length>max)throw new Error("PAYLOAD_TOO_LARGE");return request.json()}
