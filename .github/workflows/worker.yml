{"name": "Verificar y desplegar Worker", "on": {"push": {"branches": ["main"], "paths": ["worker/**", "tests/**"]}, "workflow_dispatch": null}, "jobs": {"deploy": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"uses": "actions/setup-node@v4", "with": {"node-version": 22}}, {"run": "npm test"}, {"run": "npm install", "working-directory": "worker"}, {"run": "npx wrangler d1 migrations apply biblioteca-juridica-cgt --remote", "working-directory": "worker", "env": {"CLOUDFLARE_API_TOKEN": "${{ secrets.CLOUDFLARE_API_TOKEN }}", "CLOUDFLARE_ACCOUNT_ID": "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"}}, {"run": "npx wrangler deploy", "working-directory": "worker", "env": {"CLOUDFLARE_API_TOKEN": "${{ secrets.CLOUDFLARE_API_TOKEN }}", "CLOUDFLARE_ACCOUNT_ID": "${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"}}]}}}
